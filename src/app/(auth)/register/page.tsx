import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = {
  title: "Create Account - Aegis GRC",
  description: "Create your Aegis GRC account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
